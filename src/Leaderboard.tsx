import React from "react";
import { Text, Container, Tab, Tabs } from "native-base";
import Page from "./Page";

const Leaderboard = () => (
    <Container>
        <Text>Owo I'm first</Text>
    </Container>
);

export default Leaderboard;
