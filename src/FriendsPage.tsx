import React from "react";
import { Text } from "native-base";
import Page from "./Page";

const FriendsPage = ({navigation}) => (
	<Page navigation={navigation}>
		<Text>Friends OwO</Text>
	</Page>
);

export default FriendsPage;
